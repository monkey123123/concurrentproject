<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.beanpodtech.pkms.dbadapter.dao.ITeeInfoDao">

    <!--add by Allen -->
    <resultMap id="ResultMap_SubTeeInfo" type="com.beanpodtech.pkms.common.model.tam.SubTeeInfo" >
        <id column="TEE_ID" property="teeId" jdbcType="INTEGER" />
        <result column="TEE_NAME" property="teeName" jdbcType="VARCHAR" />
        <result column="VERSION" property="version" jdbcType="INTEGER" />
        <result column="DESCRIPTION" property="description" jdbcType="VARCHAR" />
        <result column="DELETE_FLAG" property="deleteFlag" jdbcType="INTEGER" />
        <result column="CREATE_TIME" property="createTime" jdbcType="TIMESTAMP" />
        <result column="OPER_ID" property="operId" jdbcType="VARCHAR" />
        <result column="OPER_TIME" property="operTime" jdbcType="TIMESTAMP" />
    </resultMap>

    <sql id="sql_select">
        TEE_ID,
        TEE_NAME,
        VERSION,
        DESCRIPTION,
        DELETE_FLAG,
        CREATE_TIME,
        OPER_ID,
        OPER_TIME
    </sql>
    <sql id="sql_insert">
        TEE_NAME,
        VERSION,
        DESCRIPTION,
        DELETE_FLAG,
        CREATE_TIME,
        OPER_ID,
        OPER_TIME
    </sql>

    <!-- 单表 操作 useGeneratedKeys="true"会将自动生成的主键赋值给parameterType="SubTeeInfo"的实体对象 -->
    <insert id="insertSubTeeInfo" parameterType="SubTeeInfo" useGeneratedKeys="true" keyProperty="teeId">
        INSERT INTO sub_tee_info(<include refid="sql_insert"/> )
        VALUES (#{teeName},#{version},#{description},#{deleteFlag},CURRENT_TIMESTAMP,#{operId},CURRENT_TIMESTAMP )
    </insert>

    <!--not in use-->
    <update id="updateSubTeeInfo" parameterType="SubTeeInfo">
        UPDATE sub_tee_info
        SET
        <if test="teeName != null">TEE_NAME = #{teeName},</if>
        <if test="version != null">VERSION = #{version},</if>
        <if test="description != null">DESCRIPTION = #{description},</if>
        <if test="operId != null">OPER_ID = #{operId},</if>
        OPER_TIME = CURRENT_TIMESTAMP
        WHERE TEE_ID = #{teeId}
    </update>

    <!--in use-->
    <update id="updateByMap" parameterType="java.util.HashMap">
        UPDATE sub_tee_info
        SET
        <if test="teeName != null">TEE_NAME = #{teeName},</if>
        <if test="version != null">VERSION = #{version},</if>
        <if test="description != null">DESCRIPTION = #{description},</if>
        <if test="operId != null">OPER_ID = #{operId},</if>
        OPER_TIME = CURRENT_TIMESTAMP
        WHERE TEE_ID = #{teeId}
    </update>
    <!--批量逻辑删除-->
    <update id="deleteLogic" parameterType="java.util.HashMap">
        UPDATE sub_tee_info
        SET
        <if test="deleteFlag != null">DELETE_FLAG = #{deleteFlag},</if>
        <if test="operId != null">OPER_ID = #{operId},</if>
        OPER_TIME = CURRENT_TIMESTAMP
        WHERE TEE_ID IN
        <foreach collection="idList" item="itemId" index="index"
                 open="(" close=")" separator=",">
            #{itemId}
        </foreach>
    </update>

    <select id="selectById" parameterType="SubTeeInfo" resultMap="ResultMap_SubTeeInfo">
        SELECT <include refid="sql_select"/>
        FROM sub_tee_info
        where
        TEE_ID = #{teeId}
        AND DELETE_FLAG = #{deleteFlag}
    </select>

    <select id="selectByMap" parameterType="java.util.HashMap" resultMap="ResultMap_SubTeeInfo">
        SELECT <include refid="sql_select"/>
        FROM sub_tee_info
        <where>
            <if test="teeId != null">
                AND TEE_ID = #{teeId}
            </if>
            <if test="teeName != null">
                AND TEE_NAME = #{teeName}
            </if>
            <if test="deleteFlag != null">
                AND DELETE_FLAG = #{deleteFlag}
            </if>
            <if test="operId != null">
                AND OPER_ID = #{operId}
            </if>
            <if test="startTime != null">
                AND CREATE_TIME &gt;= #{startTime}
            </if>
            <if test="endTime != null">
                AND CREATE_TIME &lt;= #{endTime}
            </if>
            <if test="start != null">
                order by TEE_ID desc
                limit #{start},#{limit}
            </if>
        </where>
    </select>
    <select id="selectCountByMap" parameterType="java.util.HashMap" resultType="integer">
        SELECT count(TEE_ID)
        FROM sub_tee_info
        <where>
            <if test="teeId != null">
                AND TEE_ID = #{teeId}
            </if>
            <if test="teeName != null">
                AND TEE_NAME = #{teeName}
            </if>
            <if test="deleteFlag != null">
                AND DELETE_FLAG = #{deleteFlag}
            </if>
            <if test="operId != null">
                AND OPER_ID = #{operId}
            </if>
            <if test="startTime != null">
                AND CREATE_TIME &gt;= #{startTime}
            </if>
            <if test="endTime != null">
                AND CREATE_TIME &lt;= #{endTime}
            </if>
        </where>
    </select>


</mapper>